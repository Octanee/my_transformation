name: my_transformation
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  cloud_firestore: ^3.1.14
  cupertino_icons: ^1.0.2
  equatable: ^2.0.3
  firebase_app_check: ^0.0.6+11
  firebase_auth: ^3.3.17
  firebase_core: ^1.16.0
  flow_builder: ^0.0.6
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  flutter_inset_box_shadow: ^1.0.8
  flutter_svg: ^1.0.3
  font_awesome_flutter: ^10.1.0
  formz: ^0.4.1
  google_fonts: ^2.3.2

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - asset/
